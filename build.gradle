apply plugin: 'groovy'
apply from: "$rootDir/gradle/publishing.gradle"

repositories {
    mavenCentral()
}

dependencies {
    compile localGroovy()
    compile gradleApi()
}

task wrapper(type: Wrapper) {
    gradleVersion = "${gradleVersion}"
}